[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "tiq_intent"
version = "0.1.0"
description = "Rust intent scorer mirroring treasureiq.chat.intent_scorer exactly (PyO3)."
requires-python = ">=3.9"

[tool.maturin]
module-name = "tiq_intent"
